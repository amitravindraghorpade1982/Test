Yes. Given your clarification that business transactions are successful even when Oracle logging fails, I would revise the document so it does not imply that Oracle logging is on the business transaction critical path.

The core problem should instead be presented as:

> High-volume logging generates a very large number of concurrent Oracle writes. These writes are not required for business transaction success, but they create resource contention and scalability pressure in the logging/persistence tier.



That distinction is important for a Senior Architect review.

Below is the corrected page-by-page change list for the entire document.


---

PAGE 1 — Cover Page

Current

> Enterprise Architecture & Database Concurrency Review
IBM IIB → IBM MQ → Oracle Logging Pipeline



Change to

Enterprise Architecture & Database Concurrency Review

Current-State: IBM IIB → IBM MQ → Oracle Logging Pipeline

Target-State: IBM IIB → IBM MQ → Kafka → Scalable Logging & Analytics

Keep:

> Large Banking Enterprise — ~2,000 Services — ~300 Million Transactions/Day



But change to:

> Current Scale: ~2,000 Services | ~300 Million Transactions/Day



Why

It immediately distinguishes current architecture from target architecture.


---

PAGE 2 — Purpose & Basis of Review

Current wording

The document currently says it is intended to support a go/no-go decision on phased remediation and modernization.

Change the purpose to emphasize this:

> This review evaluates the scalability and database concurrency characteristics of the current EIS logging architecture. The business transaction is not dependent on successful completion of the logging database write; however, the high volume of concurrent logging writes creates significant resource pressure on the Oracle logging tier.



Then add:

> The objective is to identify immediate database and messaging optimizations and establish a phased target architecture that decouples high-volume logging persistence from Oracle while retaining IBM MQ as the existing enterprise messaging layer.



Very important

Add:

> A failure of an individual logging database write does not cause the associated business transaction to fail.



This makes the operating model crystal clear.


---

PAGE 3 — Table of Contents

Change Section 4

Currently:

> Section 4 —



Change to:

> Section 4 — Target-State Event-Driven Logging Architecture



Change Section 5

Current:

> Section 5 — Long-Term Enterprise Architecture



Change to:

> Section 5 — Long-Term Enterprise Target Architecture



Remove incorrect Section 6 references

Your document references Section 6 later, but there is no Section 6 in the document.

So change those references to the correct section.


---

PAGE 4 — Executive Summary

This page needs an important correction.

Current statement

The document says:

> “this design is generating extreme synchronous insert pressure directly on Oracle”



and describes the symptoms as consequences of the architecture. 

Change to:

> At approximately 300 million transactions/day, the current logging architecture generates a very high volume of concurrent Oracle insert activity. Although logging persistence is not a prerequisite for business transaction success, the resulting database workload creates significant pressure on Oracle I/O, sessions/connections, insert processing, redo/undo and storage.



Then:

> The primary architectural concern is therefore not business transaction dependency on Oracle logging, but the scalability and resource consumption of the logging persistence tier.



Add the target direction:

> The recommended target state is an asynchronous, horizontally scalable logging architecture in which IBM MQ remains the existing enterprise messaging layer and Kafka provides durable event streaming, buffering and fan-out to Oracle logging, Data Lake/analytics and future consumers.



Remove this concept

Do not say:

> “Oracle is on the critical path of the business transaction.”



Because according to your clarification, it isn't.


---

PAGE 5 — Section 1: Architecture Review

Current Flow

Keep the current diagram:

Client → IIB → IBM MQ → Oracle → Generic Analysis / Monthly Report

But rename:

> Current-State Logging Flow



Under the diagram add:

> Business transaction success is independent of successful Oracle logging persistence.



This is very important.


---

Strengths

Keep:

separation of logging concerns

IBM MQ guaranteed/ordered delivery


Add one strength

> Logging persistence failure is isolated from business transaction success, preventing an individual Oracle logging failure from directly causing business transaction failure.



Only include this if it accurately reflects the actual implementation—which you've confirmed.


---

PAGE 5 — Weaknesses

This section needs the biggest correction.

REMOVE this bullet

Current:

> Synchronous audit writes on the critical path... each awaiting commit before the flow proceeds or completes.



Replace it with:

> High-volume concurrent Oracle logging writes: Each business transaction can generate multiple logging records across API, orchestration and backend stages. The resulting aggregate write workload creates substantial concurrency and database resource pressure at ~300M transactions/day.




---

Replace the replication bullet

Current:

> Triplication of the write problem...



Keep the basic idea but change the wording to:

> Multiple logging records per business transaction: API_LOG, ORCH_API_LOG, BACKEND_LOG and associated detail/overflow records can multiply the total database write volume generated by a single business transaction.



This is more precise.


---

Keep the data-temperature point

The point about recent and historical data being stored together is valid.

I would slightly strengthen it:

> No visible data-temperature separation: Frequently accessed recent logging data and rarely accessed historical logging data are maintained within the same transactional persistence model, increasing storage, I/O and maintenance overhead.




---

Keep payload logging

Change:

> “large payloads ... expensive to write transactionally”



to:

> Large payload logging: Large payloads stored as CLOB/VARCHAR2 increase Oracle storage and write overhead even though they may be required primarily for audit, troubleshooting or historical analysis rather than transactional SQL processing.




---

PAGE 5 — Scalability Limitations

Current statement:

> Vertical scaling ... will hit a wall again at 500M–1B txn/day.



Change to:

> Vertical scaling of Oracle can provide additional capacity, but it does not fundamentally change the write amplification and concurrency characteristics of the logging workload. As transaction volume increases toward 500M–1B transactions/day, the logging tier is expected to face increasing scalability and cost challenges.



This is more defensible.


---

PAGE 6 — Immediate Actions

The three recommendations are fundamentally good. Keep them.

But change the explanation so that these are positioned as logging-tier optimizations, not business transaction latency optimizations.


---

2.1 Tune Commit Frequency

Current:

> “If flows currently commit per row...”



Change to:

> If the logging persistence flow currently commits per row, controlled batching can reduce commit frequency and associated database overhead.



Expected improvement

Instead of:

> reduce commit-related waits by 60–90%



I recommend:

> Expected improvement: Reduction in commit overhead and database write amplification, subject to batch size and workload validation.



Why?

The 60–90% number looks like a benchmark claim. Unless you have measured it, don't put it in an architecture document.


---

PAGE 6 — 2.2 DB_WRITER

Keep the recommendation.

Change:

> “causes dirty buffer pileup and write bottlenecks”



to:

> Insufficient DB writer capacity under high logging-write concurrency can contribute to dirty-buffer accumulation, write contention and uneven database throughput.



Add:

> Validate changes using Oracle wait events, DB CPU, I/O, redo generation, buffer-cache pressure and commit latency.




---

PAGE 6 — 2.3 MQ Consumer/Channel Concurrency

Keep this recommendation.

This is actually very important.

But change the wording to:

> Increase MQ consumer/channel concurrency only to the level supported by measured downstream logging persistence capacity.



Then:

> The objective is to prevent both MQ backlog accumulation and excessive concurrent pressure on the Oracle logging tier.



This makes your architecture argument much stronger.


---

PAGE 7 — Short-Term Improvements

3.1 Database

Keep:

Advanced Row Compression

Incremental partition statistics


No major changes.


---

PAGE 7 — 3.2 IIB Optimization

Current:

> Introduce a local aggregation node that batches N messages before a single multi-row insert...



I would change this.

Recommended:

> Introduce controlled aggregation/batching for logging events where supported, reducing multiple individual database writes into fewer batched persistence operations.



Add:

> Batching should be implemented in the logging/persistence path without introducing additional dependency between business transaction completion and logging persistence.



This is important.


---

PAGE 7 — 3.3 Logging Optimization

Keep the log-level tier idea.

Add:

> Where permitted by audit and regulatory requirements, high-volume payload logging should be selectively reduced, summarized or routed to lower-cost scalable storage rather than persisted as large transactional Oracle objects.




---

PAGE 7 — SECTION 4

This should become:

Section 4 — Target-State Event-Driven Logging Architecture

This is where your real architectural solution begins.

The document currently correctly describes this area as where the architectural fix begins. 


---

PAGE 7 — Kafka Recommendation

Current

> Introduce Kafka as the durable event backbone: between IIB and any persistence layer...



Change to:

> Introduce Kafka as a durable, horizontally scalable event backbone downstream of the existing IBM MQ layer. Kafka will provide buffering, replayability and fan-out to multiple downstream logging and analytics consumers.



This is very important.

It clearly says:

MQ is retained.

Kafka is not being introduced simply to replace MQ.


---

PAGE 7 — Separate Audit/Log Database

Current recommendation is good.

Change it to:

> Separate the logging/audit persistence workload from the core OLTP database. Oracle may remain as a logging persistence target where required, but logging writes should be performed asynchronously by independent consumers rather than being tightly coupled to IIB processing.



Notice the wording:

“tightly coupled to IIB processing”

rather than:

“business transaction critical path.”


---

PAGE 7 — Message Buffering

Current:

> Kafka or MQ with tuned depth...



I recommend changing it to:

> Retain IBM MQ as the existing enterprise messaging layer and introduce Kafka as the scalable event-streaming and downstream buffering/fan-out layer. Kafka consumers can independently persist, archive, analyze and enrich logging events without increasing the producer-side transaction workload.



This is the cleanest explanation of your architecture.


---

PAGE 8 — Long-Term Enterprise Architecture

This is the most important page.

Your current diagram has:

IIB → MQ → Kafka → Oracle → Redo Logs → GoldenGate → Kafka

plus another Kafka/Data Lake flow. 

The architecture needs to make the roles much clearer.

Main target flow should be:

Client

↓

IIB / EIS

↓

IBM MQ

↓

Kafka

↓

Parallel consumers:

Oracle Logging Consumer → Oracle

Data Lake Consumer → Iceberg / Parquet

Analytics Consumer → Trino / Spark

Future Consumers / AI


---

PAGE 8 — Oracle

Do not make Oracle appear to be the central destination of Kafka.

Label it:

> Oracle — Logging Persistence (Asynchronous Consumer)



That one label will eliminate a lot of confusion.


---

PAGE 8 — Oracle Redo / GoldenGate

Do not remove GoldenGate if it is required.

But visually separate it from the primary logging flow.

Show:

Oracle

↓

Oracle Redo Logs

↓

GoldenGate

↓

CDC / Downstream Replication

And label it:

> CDC / Replication Path



This makes it clear that GoldenGate is not the mechanism that solves the Oracle write bottleneck.


---

PAGE 8 — Data Lake

Your existing:

> Kafka → Hadoop/Iceberg Data Lake → Transactions Parquet → Trino → Generic Analysis / Monthly Report



is directionally good. 

Make the separation explicit:

Operational / logging persistence

Kafka → Oracle Consumer → Oracle

Historical / high-volume analytics

Kafka → Data Lake/Iceberg → Parquet → Trino/Spark

This is a much stronger architecture.


---

PAGE 8 — Add Retry/DLQ

This is currently missing.

Add:

Kafka → Oracle Consumer

with:

Retry Topic → DLQ

The concept should be:

> Temporary Oracle failure does not result in loss of logging events. Events remain available in Kafka and are retried by the persistence consumer.



This fits perfectly with your actual business requirement because logging failure must not affect business transaction success.


---

PAGE 8 — Scalability

Keep the existing Kafka scaling point.

Add:

> Consumer capacity can be independently scaled based on Kafka partition utilization, consumer lag and downstream persistence capacity.




---

PAGE 8 — Availability & Fault Tolerance

Keep replication factor 3.

Add:

> Independent consumer groups allow Oracle persistence, Data Lake ingestion and analytics processing to scale and recover independently.




---

PAGE 8 — Disaster Recovery

Current statement about replayability is good.

Change to:

> The event backbone provides replay capability for logging workloads, allowing downstream consumers to recover or rebuild persistence without requiring a full Oracle logging database standby solely for replay purposes.



Then add:

> Replay, retention and recovery must be governed by defined RPO/RTO, offset management and idempotent consumer design.




---

PAGE 8 — Observability

Keep the existing section.

Add these metrics:

MQ

Queue depth

Message age

Consumer throughput


Kafka

Partition utilization

Consumer lag

Producer throughput

Consumer throughput

Retry count

DLQ count


Oracle

DB CPU

I/O

Sessions/connections

Commit latency

Redo generation

Wait events


This will make the target architecture operationally credible.


---

PAGE 9 — Best Practices

First bullet

Current:

> Fire-and-forget publish of log events to MQ/Kafka from within the flow...



This is ambiguous because it gives MQ and Kafka equal roles.

Change to:

> Publish logging events through the existing IBM MQ layer and subsequently stream them through Kafka for scalable downstream processing, while keeping business transaction completion independent of logging persistence.




---

Aggregation

Keep:

> Aggregation nodes to batch multiple log events...



Change to:

> Aggregate multiple logging events at the persistence consumer to reduce the number of individual Oracle writes.



This is important.


---

Caching

Keep the reference-data caching recommendation.

No major change.


---

PAGE 9 — Anti-Patterns

The current anti-patterns are good, but change the first one.

Current

> Synchronous JDBC insert-stage embedded directly in the business flow — couples customer-facing latency to Oracle's write latency.



REMOVE THIS.

It is not applicable based on your clarification.

Replace with:

> High-volume logging persistence tightly coupled to IIB processing: Even when business transaction success is independent of logging success, excessive logging write activity can consume shared database, connection and infrastructure resources.




---

Keep this:

> Multiple independent inserts per stage instead of one batched/aggregated write.



This is still valid.

Change the third:

Current:

> No visible circuit-breaker/back-pressure handling ... risk of flow thread pool exhaustion cascading back to client channel.



Since you said business transactions aren't dependent on DB logging, don't imply this cascade unless you have evidence of it.

Change to:

> Insufficient back-pressure and consumer throttling in the logging pipeline can result in MQ/Kafka backlog growth and excessive downstream database concurrency.



Much safer.


---

PAGE 9 — How to Reduce Oracle Writes

This section should become the final architectural conclusion.

Point 1

Change to:

> Decouple logging persistence from IIB processing by publishing logging events through IBM MQ/Kafka and processing them asynchronously.



Point 2

Keep:

> Aggregate multiple logging events into fewer, wider batch inserts at the consumer.



Point 3

Keep:

> Cache and reference rather than repeatedly writing unchanged metadata.



Add Point 4

> Move large, infrequently queried payloads and historical logging data to scalable Data Lake/object storage where permitted by audit and retention requirements.




---

ADD THIS FINAL SECTION TO PAGE 9

I strongly recommend ending the document with:

Key Architectural Principles

1. Business transaction success is independent of logging database persistence success.


2. IBM MQ remains the existing enterprise messaging layer.


3. Kafka provides durable, scalable event streaming, buffering and downstream fan-out.


4. Oracle logging persistence is moved to independently scalable asynchronous consumers.


5. Oracle write concurrency is controlled by consumer capacity rather than producer transaction volume.


6. Data Lake/Iceberg provides scalable storage for high-volume historical logging and analytics.


7. Retry, DLQ, replay and idempotency protect logging data without impacting business transaction success.




---

The most important terminology change throughout the document

I would search the entire document for these terms:

❌ Avoid

“synchronous Oracle logging”

“Oracle on the critical path”

“business transaction waits for Oracle”

“customer-facing latency to Oracle”

“Oracle failure causes transaction failure”

“commit before the business flow completes”


✅ Use instead

high-volume concurrent Oracle logging writes

logging persistence workload

Oracle logging resource contention

logging persistence tier

asynchronous logging persistence

decoupled logging pipeline

downstream persistence consumer

logging write concurrency

database write amplification

MQ/Kafka buffering

consumer-controlled persistence



---

The single architectural story your document should tell

The whole document should ultimately lead the reviewer to this:

CURRENT

Business Transaction

Client → IIB → IBM MQ → Business Processing → SUCCESS

Logging Workload

IIB → IBM MQ → Oracle

The business transaction does not depend on Oracle logging success, but the logging workload creates very high Oracle write concurrency.

TARGET

Client → IIB → IBM MQ → Business Processing → SUCCESS

and independently:

Logging Events → Kafka →

Oracle Consumer → Oracle

Data Lake Consumer → Iceberg/Parquet

Analytics Consumer → Trino/Spark

Future Consumer → AI/Other

with:

Kafka → Retry → DLQ

and separately:

Oracle → Redo → GoldenGate → CDC

That is, in my view, the corrected architectural narrative for your actual EIS behavior. It preserves your MQ, introduces Kafka for scalable downstream processing, and—most importantly—does not incorrectly claim that Oracle logging controls business transaction success. 
